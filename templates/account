<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Account</title>
</head>
<body>
    <div>
        <p>User data</p>
        {% for label, value in uData.items() %}
            <p>{{label}}: <dd>{{value}}</dd></p>
        {% endfor %}
    </div>
    <div>

    </div>
    <div>
        <p><a href="{{url_for("choicefunc", func='setPassword')}}">change password</a></p>
    </div>
</body>
</html>